import { Check, RotateCcw, Square, X } from 'lucide-react'

export default function AgentApprovalBar({ status, busy, mutationBlockedReason, onCancel, onReject, onApply, onUndo }: { status: string; busy: boolean; mutationBlockedReason?: string; onCancel: () => void; onReject: () => void; onApply: () => void; onUndo: () => void }) {
  if (['queued', 'planning', 'gathering_context', 'drafting', 'validating', 'applying'].includes(status)) return <div className="sticky bottom-0 border-t border-slate-200 bg-white p-3"><button aria-label="取消任务" onClick={onCancel} className="inline-flex min-h-10 w-full items-center justify-center gap-2 rounded-lg border border-slate-200 text-sm font-medium text-slate-700 hover:bg-slate-50"><Square className="h-4 w-4" />取消任务</button></div>
  if (status === 'awaiting_approval') return <div className="sticky bottom-0 border-t border-slate-200 bg-white p-3">{mutationBlockedReason && <p className="mb-2 rounded-md bg-amber-50 px-3 py-2 text-xs leading-5 text-amber-800" role="status">{mutationBlockedReason}</p>}<div className="grid grid-cols-2 gap-2"><button aria-label="放弃草案" onClick={onReject} className="inline-flex min-h-10 items-center justify-center gap-1.5 rounded-lg border border-slate-200 text-xs font-semibold text-slate-700 hover:bg-slate-50"><X className="h-4 w-4" />放弃草案</button><button aria-label="应用变更" disabled={busy || Boolean(mutationBlockedReason)} onClick={onApply} className="inline-flex min-h-10 items-center justify-center gap-1.5 rounded-lg bg-dream-600 text-xs font-semibold text-white hover:bg-dream-700 disabled:cursor-not-allowed disabled:opacity-50"><Check className="h-4 w-4" />应用变更</button></div></div>
  if (status === 'completed') return <div className="sticky bottom-0 border-t border-slate-200 bg-white p-3">{mutationBlockedReason && <p className="mb-2 rounded-md bg-amber-50 px-3 py-2 text-xs leading-5 text-amber-800" role="status">{mutationBlockedReason}</p>}<button aria-label="撤销变更" disabled={busy || Boolean(mutationBlockedReason)} onClick={onUndo} className="inline-flex min-h-10 w-full items-center justify-center gap-2 rounded-lg border border-dream-200 text-sm font-semibold text-dream-700 hover:bg-dream-50 disabled:cursor-not-allowed disabled:opacity-50"><RotateCcw className="h-4 w-4" />撤销本次变更</button></div>
  return null
}
