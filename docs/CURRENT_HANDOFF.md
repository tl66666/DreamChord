# DreamChord 当前迭代交接

更新日期：2026-08-12

这份文档给第一次接触 DreamChord 的维护者、面试官和后续编码 Agent 使用。它记录当前工作区已经完成但尚未单独发布版本号的一轮改动，以及继续迭代时应保持的边界。

## 先读什么

| 目的 | 入口 |
|---|---|
| 理解产品与可演示能力 | [项目概览](PROJECT_OVERVIEW.md) |
| 上手创作与素材 | [创作者工作流](CREATOR_WORKFLOW.md) |
| 使用创作 Agent | [Agent 使用指南](AGENT_GUIDE.md) |
| 理解长期架构 | [架构与 AI 交接](AI_HANDOFF.md) |
| 继续开发或发布 | [贡献指南](../CONTRIBUTING.md) / [发布指南](RELEASE_GUIDE.md) |

## 本轮完成内容

### 0. 镜头级音频演出（2026-08-13）

- 素材库新增 `SFX` 与 `VOICE` 分类，和既有 `BGM` 共用真实媒体内容检测、安全扩展名存储、跨项目复用、重命名、替换、删除及项目备份/恢复路径。
- 镜头卡把 `audio` 作为可播放节点数据保存：BGM 可保持、播放/切换或停止；音效是一次性列表；配音绑定到当前对话或旁白。镜头卡、节点图、运行时和播放器使用同一结构化链路，官方演示与用户项目没有分叉实现。
- 播放器以循环 BGM、单段配音和一次性音效三条通道播放，并持久化 BGM/音效/配音三个音量设置；重开与页面卸载会清理运行时媒体。
- Agent 的素材读取工具覆盖三类音频。无 Key 本地助手会盘点现有音频、说明缺口和作者绑定边界；不创建剧情补丁、不自动套用或替换音频。
- 这一轮不包含在线 TTS、音乐生成、波形剪辑、逐字字幕同步或完整多轨时间线，避免把未实现的外部生产能力包装为已完成特性。
- 新增桌面端真实验收截图 `docs/screenshots/audio-direction-editor-1440.png` 与 `docs/screenshots/audio-player-settings-1440.png`，分别展示镜头卡的音频演出区和播放器的三通道音量设置。

### 0.1 角色声音档案与 Agent 配音规划（2026-08-13）

- 新增项目级 `VOICE_SAMPLE`，上传时必须选择当前项目角色并确认拥有声音使用与声线生成授权；服务端把归属与演绎说明存入素材元数据，仍使用 MP3/WAV/OGG 实际内容检测。
- `VOICE_SAMPLE` 不是播放器素材，也不等于本地声纹克隆模型；只有 `VOICE` 才能绑定镜头卡。项目备份会携带样本元数据，并在导入时重映射角色 ID。
- Agent 新增只读 `plan_character_voice` 工具和 `voice-plan` 策略：按角色、章节或场景列出台词、情绪、语速、停顿、授权样本、Provider 缺口和下一步。无 API Key/Provider 时可完成规划，但不能生成或伪造音频。
- 后续接入 Provider 的固定边界是：请求生成候选 `VOICE` -> 作者试听 -> 作者确认 -> 镜头音频绑定。不要直接把样本发给任意模型、不要自动替换已有配音。

### 0. 故事工作台、通用播放器与 CI 完整性（2026-08-12）

- 审计并纳入此前未提交的工作台改动：场景与镜头卡编辑、分支去向提示、流程图防遮挡与自动换行、项目体检、播放器跳过和多角色排布，以及九场景官方演示剧情。
- 播放器改为通过项目 API 获取角色默认立绘和表情 URL。用户上传并绑定到项目的立绘不再依赖浏览器本地缓存，导入项目和另一台电脑上的播放行为保持一致。
- GitHub CI 使用隔离 SQLite 数据库，按共享剧情包构建、Prisma 生成、迁移、演示数据种子、lint、测试和生产构建的顺序执行。
- `d4f17ca` 对应的 CI 与 GitHub Pages 部署均已成功。历史 Actions 失败记录保留在 GitHub 时间线中，判断当前状态必须查看最新提交。
- 新增 `docs/MAINTAINER_PLAYBOOK.md`，供后续维护者按相同边界进行验收、开发、验证与发布。

### 0. Agent 策略与创作简报重构（2026-08-12）

- 新增纯 `AgentPolicy`，将本地即时回答、只读对话、受控剧情行动、已选草稿导入和素材规划的路由规则从任务服务中抽离；
- 新增 `CreativeBrief`，让“只生成素材提示词”从原始创作请求导出可复用素材、缺少素材和提示词，而不是丢失剧情语义后使用固定模板；
- 素材只做保守精确匹配，未匹配的背景、角色或 CG 会进入缺少清单，不会自动绑定无关项目素材；
- 保持既有 StoryPatch 预览、作者确认、应用与撤销机制。素材规划本身不创建补丁，也不写入工作台；
- 当前任务执行仍是本地进程内队列。服务重启会安全停止活动任务；未来云端部署可替换为持久队列，但尚未实现。

### 1. Agent 无 Key 与有 Key 的双路径

没有 API Key 时，系统不再把 Agent 退化成“请先配置模型”的提示。服务端使用本地意图路由和项目快照完成确定性任务：项目/章节/角色/素材盘点、剧情体检、创作知识问答、素材提示词、确定性续写草稿，以及用户正文或已选 Agent 草稿的结构化转换。

配置 GLM、DeepSeek、Kimi、OpenAI 或兼容模型后，模型负责更自然的长文续写、对白润色、开放式改写和复杂剧情规划。两种模式都不能直接写入故事，统一经过提案、校验、预览、作者确认和版本安全撤销。

### 2. 正文到工作台的结构化导入

已选草稿会先过滤“续写草稿”“镜头卡说明”“节点连线”等非故事文字，再按下列标记解析：

- `镜头`：拆分场景组；
- `地点`：设置场景标题并尝试匹配项目背景；
- `旁白`、`画面`、`动作`、`音效`：生成旁白卡；
- `角色`：生成角色对话卡，项目中存在同名角色时补充登场节点；
- `选项`：生成选项节点。

匹配采用保守策略。项目内没有精确匹配的背景或角色时，保留文字与结构，不自动套用默认素材。多镜头导入的回归测试还会把补丁应用到内存图，确认节点和连线可通过领域校验。

主要文件：

- `apps/server/src/agent/localAssistant.ts`
- `apps/server/src/agent/localAssistant.test.ts`
- `apps/server/src/agent/runService.ts`
- `packages/story-domain/src/patch.ts`

### 3. Agent 工作流与模型配置体验

- Agent 可新建、搜索、重命名、置顶、删除独立对话；对话可以不绑定章节，也可以绑定后进入受控创作。
- 模型设置页保留来源页面，配置后可返回原 Agent 对话。
- 设置页区分“未启用”“已启用但缺 Key”“Key 已保存在本机”。这里的“已保存”只表示本地配置存在，不代表模型连通性已经验证。
- 右侧信息区使用“创作参考、故事资料、长期设定”等创作语言，不直接暴露工具策略和原始产物 ID。

### 4. 启动、文档和展示材料

- Windows 启动器不再结束任何已有监听进程；端口被占用时会选择候选端口继续启动。
- 新增真实桌面验收截图 `docs/screenshots/agent-structured-import-1440.png`，展示“选中草稿 -> 清洗/解析 -> 补丁预览 -> 应用或放弃”。
- README、项目概览、Agent 指南、展示页和个人网站 DreamChord 项目卡都已同步无 Key/有 Key 边界和结构化导入能力。

## Agent 的工具与外部访问边界

### 内部 Tool Calling：有，且仅限业务白名单

当前注册 15 个内部工具，运行在 DreamChord 服务端，不是外部插件：

| 分类 | 工具 |
|---|---|
| 项目与剧情读取 | `read_project_brief`、`read_chapter_outline`、`read_scene`、`search_story`、`analyze_story_graph` |
| 对话与记忆 | `read_conversation_context`、`search_memories` |
| 角色与素材读取 | `list_project_assets`、`inspect_asset`、`read_character_profile` |
| 剧情提案校验 | `create_story_patch`、`validate_story_patch` |
| 素材候选处理 | `prepare_character_asset`、`prepare_cg_asset`、`prepare_background_asset` |

每个输入经过 Zod Schema 校验；剧情补丁进一步经 `@dreamchord/story-domain` 校验。工具只能读取授权项目资源或生成候选/提案，不能绕过作者确认写入数据库。

### 外部网络访问：只有两种明确场景

| 类型 | 是否需要 API Key | 实际访问 | 发送内容 | 限制 |
|---|---|---|---|---|
| 模型推理 | 需要 | 用户配置的 OpenAI 兼容 `/chat/completions` 接口 | 当前任务和必要项目上下文 | Provider URL 会做 DNS、私网地址和重定向检查；Key 不写入消息、运行记录或数据库 |
| 公共知识查询 | 不需要 | `https://zh.wikipedia.org/api/rest_v1/page/summary/...` | 清洗后的问题关键词 | 只允许中文 Wikipedia、只读 GET、3.5 秒超时、响应长度限制；不发送故事、记忆或密钥 |

本地图片检查、白底去除、裁边和衍生图由 Sharp 在本机完成，不调用外部图像服务。

### 明确没有提供的能力

Agent 没有 MCP Server 集成，也没有浏览器自动化、Shell、任意文件系统、SQL、任意 HTTP URL 或任意代码执行工具。这样做是为了避免创作 Agent 以“帮用户写剧情”为名越过本地数据与网络边界。

## 关键运行链路

```text
用户消息
  -> 识别任务与绑定范围
  -> 装配项目、章节、场景、角色、素材、对话摘要和记忆
  -> 本地确定性助手 或 OpenAI 兼容模型
  -> 白名单工具调用 / 本地结构化解析
  -> StoryPatch + 图校验
  -> 预览
  -> 作者应用、放弃或撤销
```

普通聊天不会生成剧情补丁；只有绑定章节且存在明确修改意图时，才允许进入受控行动。已选草稿导入为了避免模型重写原文，会强制走确定性本地解析器。

## 已验证内容

本轮完成后已运行：

```bash
pnpm lint
pnpm test
pnpm -r build
pnpm test:readiness
git diff --check
```

当前全量测试结果：领域包 12 项、服务端 182 项、前端 98 项。浏览器还验证了 Agent 草稿到补丁预览、镜头卡音频演出与播放器三通道音量设置的桌面流程，以及个人网站和项目展示页的桌面呈现。

Windows 上运行全量测试前，应先停止当前 DreamChord 后端；否则 Prisma 可能无法替换 `query_engine-windows.dll.node`。不要通过删除数据库或重置仓库处理该问题。

## 已知边界

- 本地续写是项目上下文驱动的确定性草稿，不等同于外部大模型的开放式文风创作。
- 正文解析是结构化标记和规则解析，不是通用语义剧本理解；复杂动作、镜头语言和分支后续仍需作者检查。
- 只有提供了后续场景的选项才能自动连接完整分支；仅导入选项文本时，编辑器会保留选项卡并由作者补充分支去向。
- 素材自动绑定坚持精确匹配，宁可留空也不使用不相关立绘或背景。
- 当前产品定位为本地单用户创作工作台，不包含多人实时协作、通用语义抠图、在线配音生产线或完整多轨 BGM 时间线；已支持镜头级 BGM、音效和手动配音绑定。

## 后续迭代建议

按优先级建议继续做以下工作：

1. 扩展剧本中间表示：支持角色退场、表情、站位、转场、镜头动作和选项后的分支正文；为现有音频增加循环点、淡入淡出、波形预览与字幕同步，仍保持“不能可靠识别就不自动写入”的策略。
2. 为 Agent 建立评测集：覆盖无 Key 回答、模型工具调用、草稿导入、素材匹配、补丁合法性、失败降级和撤销，避免功能越多越难验证。
3. 增加模型连通性测试入口：保存配置后可由用户主动测试 Provider 可用性、模型名和错误原因，但不要把 API Key 持久化到服务端数据库。
4. 提供素材候选匹配面板：当背景或角色只有近似匹配时，向作者展示候选和理由，由作者选择，不把模糊匹配直接写入故事。
5. 在保留本地单用户边界的前提下，逐步评估 PostgreSQL、对象存储和协作编辑的部署模型；这应作为独立架构改造，不和日常故事编辑功能混在一起。

## 接手检查清单

1. 阅读本文件、`docs/AI_HANDOFF.md` 与 `CLAUDE.md`。
2. 运行 `start-dreamchord.bat`，使用 `demo / demo123` 登录。
3. 在 Agent 中不绑定章节询问项目概况，再绑定章节生成或粘贴一份草稿。
4. 选择“生成工作台场景”，检查补丁预览、素材匹配和放弃/应用入口。
5. 打开编辑器确认导入场景的旁白、台词、选项和流程图结构。
6. 提交前运行完整质量检查，并确认 `.env`、数据库、上传文件和日志没有进入 Git。
